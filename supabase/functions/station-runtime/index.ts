import { createClient, type SupabaseClient } from 'npm:@supabase/supabase-js@2'

const corsHeaders={'Access-Control-Allow-Origin':'*','Access-Control-Allow-Headers':'authorization, x-client-info, apikey, content-type, x-aezakmi-station-id, x-aezakmi-station-token','Access-Control-Allow-Methods':'POST,OPTIONS'}
function preflight(req:Request){if(req.method==='OPTIONS')return new Response('ok',{headers:corsHeaders});return null}
function json(value:unknown,status=200){return new Response(JSON.stringify(value),{status,headers:{...corsHeaders,'Content-Type':'application/json'}})}
function fail(error:any,fallback='Request failed.'){const status=Number(error?.status||500);return json({success:false,code:error?.code||'SERVER_ERROR',error:status>=500?fallback:(error?.message||fallback)},status)}
async function sha256(value:string){const digest=await crypto.subtle.digest('SHA-256',new TextEncoder().encode(value));return Array.from(new Uint8Array(digest)).map(b=>b.toString(16).padStart(2,'0')).join('')}
function namedKey(envName:string){const raw=Deno.env.get(envName)||'';if(!raw)return'';try{const parsed=JSON.parse(raw);if(parsed&&typeof parsed==='object')return String(parsed.default||Object.values(parsed)[0]||'')}catch{}return''}
function projectUrl(){const v=Deno.env.get('SUPABASE_URL')||'';if(!v)throw Object.assign(new Error('SUPABASE_URL unavailable.'),{status:500});return v.replace(/\/+$/,'')}
function secretKey(){const v=Deno.env.get('SUPABASE_SECRET_KEY')||namedKey('SUPABASE_SECRET_KEYS')||Deno.env.get('SUPABASE_SERVICE_ROLE_KEY')||'';if(!v)throw Object.assign(new Error('Supabase privileged key is unavailable.'),{status:500});return v}
function adminClient(){return createClient(projectUrl(),secretKey(),{auth:{persistSession:false,autoRefreshToken:false}})}
async function stationAuth(req:Request,admin:SupabaseClient){const id=String(req.headers.get('x-aezakmi-station-id')||'').trim(),token=String(req.headers.get('x-aezakmi-station-token')||'');if(!id||!token)throw Object.assign(new Error('Customer Station credentials are required.'),{status:401,code:'STATION_AUTH_REQUIRED'});const hash=await sha256(token);const{data,error}=await admin.from('station_devices').select('id,organization_id,branch_id,local_station_id,station_name,realtime_topic_key,status,revoked_at,cloud_last_seen_at').eq('id',id).eq('device_token_hash',hash).maybeSingle();if(error)throw error;if(!data||data.revoked_at)throw Object.assign(new Error('Customer Station credential is invalid or revoked.'),{status:401,code:'STATION_AUTH_INVALID'});const{data:org,error:orgError}=await admin.from('organizations').select('lifecycle_status').eq('id',data.organization_id).maybeSingle();if(orgError)throw orgError;if(!org||!['active','grace_period'].includes(String(org.lifecycle_status||'active')))throw Object.assign(new Error(org?.lifecycle_status==='terminated'?'This business has been terminated.':'Cloud access for this business is suspended. Use Café Edge fallback.'),{status:403,code:org?.lifecycle_status==='terminated'?'BUSINESS_TERMINATED':'BUSINESS_SUSPENDED'});return data}
async function cloudSessionAction(admin:SupabaseClient,station:any,action:string,payload:any,operationKey:string|null=null){const{data,error}=await admin.rpc('aezakmi_cloud_execute',{p_branch_id:station.branch_id,p_action:action,p_payload:payload||{},p_actor_kind:'station',p_actor_id:station.id,p_operation_key:operationKey});if(error)throw error;return data}
async function rollbackLockCheckpoint(admin:SupabaseClient,station:any,commandId:string){const{data,error}=await admin.rpc('aezakmi_rollback_station_lock',{p_branch_id:station.branch_id,p_pc_id:station.local_station_id,p_command_id:commandId,p_resumed_at:new Date().toISOString()});if(error)throw error;return data}
async function restoreStationAvailable(admin:SupabaseClient,station:any){const{data:active,error:activeError}=await admin.from('branch_sessions').select('local_id').eq('branch_id',station.branch_id).eq('pc_id',station.local_station_id).eq('status','active').limit(1).maybeSingle();if(activeError)throw activeError;if(active)return;const{data:row,error:rowError}=await admin.from('branch_stations').select('status').eq('branch_id',station.branch_id).eq('local_id',station.local_station_id).maybeSingle();if(rowError)throw rowError;const current=String(row?.status||'').toLowerCase();if(!['maintenance','reserved'].includes(current)){const{error}=await admin.from('branch_stations').update({status:'available',updated_at:new Date().toISOString()}).eq('branch_id',station.branch_id).eq('local_id',station.local_station_id);if(error)throw error}}
async function broadcastEdge(topicKey:string,payload:any){if(!topicKey)return;try{await fetch(`${projectUrl()}/realtime/v1/api/broadcast/${encodeURIComponent(`edge-wakeup:${topicKey}`)}/events/sync`,{method:'POST',headers:{apikey:secretKey(),'Content-Type':'application/json'},body:JSON.stringify(payload||{})})}catch{}}
async function mirrorStationStateToEdge(admin:SupabaseClient,station:any,command:any,result:any){if(!['lock','unlock','reboot','shutdown'].includes(String(command.command)))return;const{data:edge,error}=await admin.from('edge_servers').select('id,realtime_topic_key').eq('branch_id',station.branch_id).is('revoked_at',null).order('last_seen_at',{ascending:false}).limit(1).maybeSingle();if(error||!edge)return;const inserted=await admin.from('cloud_commands').insert({organization_id:station.organization_id,branch_id:station.branch_id,edge_id:edge.id,station_id:station.local_station_id,command:'station_state',payload:{stationCommandId:command.id,command:command.command,result:result&&typeof result==='object'?result:{}},expires_at:new Date(Date.now()+120000).toISOString()});if(!inserted.error)await broadcastEdge(edge.realtime_topic_key,{kind:'station_state',stationId:station.local_station_id,command:command.command})}

Deno.serve(async req=>{const pre=preflight(req);if(pre)return pre;try{const admin=adminClient(),body=await req.json().catch(()=>({})),action=String(body.action||'heartbeat'),now=new Date().toISOString();
  if(action==='heartbeat'){
    const id=String(req.headers.get('x-aezakmi-station-id')||'').trim(),token=String(req.headers.get('x-aezakmi-station-token')||'')
    if(!id||!token)throw Object.assign(new Error('Customer Station credentials are required.'),{status:401,code:'STATION_AUTH_REQUIRED'})
    const localIpRaw=String(body.localIp||'').trim()
    const localIp=/^(?:\d{1,3}\.){3}\d{1,3}$/.test(localIpRaw)&&localIpRaw.split('.').every((part:string)=>Number(part)>=0&&Number(part)<=255)?localIpRaw:null
    const softwareVersion=String(body.softwareVersion||'').trim().slice(0,64)||null
    const{data,error}=await admin.rpc('aezakmi_station_runtime_heartbeat',{p_station_device_id:id,p_device_token_hash:await sha256(token),p_local_ip:localIp,p_software_version:softwareVersion,p_recovered_from_fallback:Boolean(body.recoveredFromFallback),p_used_fallback:Boolean(body.usedFallback)})
    if(error)throw error
    if(!data||data.success===false)throw Object.assign(new Error(data?.error||'Customer Station heartbeat failed.'),{status:Number(data?.status||500),code:data?.code||'STATION_HEARTBEAT_FAILED'})
    return json(data)
  }
  const station=await stationAuth(req,admin)
  if(action==='unpair'){
    // Customer-side Reset Pairing must revoke the Cloud identity before the
    // Electron app deletes its local credential. This prevents ghost pairings
    // where the kiosk looks unpaired but Supabase still owns the logical PC.
    const{data:release,error:releaseError}=await admin.rpc('aezakmi_station_release_session',{p_branch_id:station.branch_id,p_pc_id:station.local_station_id,p_reason:'station_unpair',p_interrupted_at:now,p_expected_member_id:null});if(releaseError)throw releaseError;if(release&&typeof release==='object'&&release.success===false)throw Object.assign(new Error(release.error||'Unable to save the active station session before unpairing.'),{status:Number(release.status||400),code:release.code||'STATION_INTERRUPTION_FAILED'});
    await admin.from('branch_customer_auth_sessions').update({revoked_at:now}).eq('branch_id',station.branch_id).eq('station_device_id',station.id).is('revoked_at',null);
    await admin.from('station_commands').update({status:'failed',acknowledged_at:now,result:{error:'Customer Station pairing was reset on the client.',code:'STATION_UNPAIRED'}}).eq('station_device_id',station.id).in('status',['queued','running']);
    const{data:stationState,error:stationStateError}=await admin.from('branch_stations').select('status').eq('branch_id',station.branch_id).eq('local_id',station.local_station_id).maybeSingle();if(stationStateError)throw stationStateError;const persistedStatus=String(stationState?.status||'offline').toLowerCase();
    const stationPatch=await admin.from('branch_stations').update({station_device_id:null,cloud_connection_status:'unpaired',cloud_last_seen_at:null,status:['maintenance','reserved'].includes(persistedStatus)?persistedStatus:'offline',updated_at:now}).eq('branch_id',station.branch_id).eq('local_id',station.local_station_id).eq('station_device_id',station.id);
    if(stationPatch.error)throw stationPatch.error;
    const revoked=await admin.from('station_devices').update({status:'revoked',revoked_at:now,updated_at:now}).eq('id',station.id).is('revoked_at',null);
    if(revoked.error)throw revoked.error;
    return json({success:true,unpaired:true,stationId:station.id,localStationId:station.local_station_id})
  }
  if(action==='poll'){
    const{data:expired,error:expiredError}=await admin.from('station_commands').select('id,command,status').eq('station_device_id',station.id).in('status',['queued','running']).lte('expires_at',now);if(expiredError)throw expiredError;for(const command of expired||[]){if(command.command==='lock')await rollbackLockCheckpoint(admin,station,command.id);if(command.command==='reboot'||command.command==='shutdown')await restoreStationAvailable(admin,station);const{error:updateExpiredError}=await admin.from('station_commands').update({status:'expired',acknowledged_at:now,result:{error:'Station command expired before delivery.',code:'COMMAND_EXPIRED'}}).eq('id',command.id).eq('status',command.status);if(updateExpiredError)throw updateExpiredError}const{data,error}=await admin.from('station_commands').select('id,command,payload,status,requested_at,expires_at').eq('station_device_id',station.id).in('status',['queued','running']).gt('expires_at',now).order('requested_at',{ascending:true}).limit(20);if(error)throw error;return json({success:true,commands:data||[],serverTime:now})
  }
  if(action==='ack'){
    const commandId=String(body.commandId||''),status=String(body.status||'');if(!commandId||!['running','completed','failed'].includes(status))return json({success:false,code:'ACK_INVALID',error:'Command id and valid status are required.'},400);const{data:command,error:commandError}=await admin.from('station_commands').select('id,command,status,station_device_id').eq('id',commandId).eq('station_device_id',station.id).maybeSingle();if(commandError)throw commandError;if(!command)return json({success:false,code:'COMMAND_NOT_FOUND',error:'Station command was not found.'},404);if(['completed','failed','expired'].includes(command.status))return json({success:true,command:{id:command.id,status:command.status},duplicate:true});const allowed=command.status==='queued'?['running','failed','completed']:command.status==='running'?['completed','failed']:[];if(!allowed.includes(status))return json({success:false,code:'COMMAND_STATE_CONFLICT',error:`Command cannot move from ${command.status} to ${status}.`},409);const result=body.result&&typeof body.result==='object'?body.result:{};if(status==='failed'&&command.command==='lock')await rollbackLockCheckpoint(admin,station,command.id);const patch:any={status,result};if(status==='completed'||status==='failed')patch.acknowledged_at=now;const{error:updateError}=await admin.from('station_commands').update(patch).eq('id',command.id).eq('status',command.status);if(updateError)throw updateError;if(status==='completed'){if(command.command==='lock')await cloudSessionAction(admin,station,'session.pause_pc',{pcId:station.local_station_id,reason:'station_lock',commandId:command.id},`station-command:${command.id}:pause`);if(command.command==='unlock')await cloudSessionAction(admin,station,'session.resume_pc',{pcId:station.local_station_id,commandId:command.id},`station-command:${command.id}:resume`);await mirrorStationStateToEdge(admin,station,command,result)}else if(status==='failed'&&(command.command==='reboot'||command.command==='shutdown'))await restoreStationAvailable(admin,station);return json({success:true,command:{id:command.id,status}})
  }
  return json({success:false,code:'INVALID_ACTION',error:'Unsupported station runtime action.'},400)
}catch(e){return fail(e,'Customer Station cloud runtime failed.')}})
