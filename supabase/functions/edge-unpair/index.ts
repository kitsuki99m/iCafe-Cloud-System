import{preflight}from'../_shared/cors.ts';import{json,fail}from'../_shared/response.ts';import{adminClient}from'../_shared/supabase.ts';import{requireEdge}from'../_shared/edge.ts';Deno.serve(async req=>{const o=preflight(req);if(o)return o;try{const admin=adminClient(),edge=await requireEdge(req,admin),now=new Date().toISOString();await admin.from('edge_servers').update({revoked_at:now,updated_at:now}).eq('id',edge.id);return json({success:true})}catch(e){return fail(e,'Unable to unpair Edge server.')}})
