import { createClient } from 'npm:@supabase/supabase-js@2'

const corsHeaders={'Access-Control-Allow-Origin':'*','Access-Control-Allow-Headers':'authorization, x-client-info, apikey, content-type','Access-Control-Allow-Methods':'POST,OPTIONS'}
function preflight(req:Request){if(req.method==='OPTIONS')return new Response('ok',{headers:corsHeaders});return null}
function json(value:unknown,status=200){return new Response(JSON.stringify(value),{status,headers:{...corsHeaders,'Content-Type':'application/json'}})}
function fail(error:any,fallback='Request failed.'){const status=Number(error?.status||500);return json({success:false,code:error?.code||'SERVER_ERROR',error:status>=500?fallback:(error?.message||fallback)},status)}
async function sha256(value:string){const digest=await crypto.subtle.digest('SHA-256',new TextEncoder().encode(value));return Array.from(new Uint8Array(digest)).map(b=>b.toString(16).padStart(2,'0')).join('')}
function token(bytes=32){const v=new Uint8Array(bytes);crypto.getRandomValues(v);return btoa(String.fromCharCode(...v)).replace(/\+/g,'-').replace(/\//g,'_').replace(/=+$/,'')}
function namedKey(envName:string){const raw=Deno.env.get(envName)||'';if(!raw)return'';try{const parsed=JSON.parse(raw);if(parsed&&typeof parsed==='object')return String(parsed.default||Object.values(parsed)[0]||'')}catch{}return''}
function projectUrl(){const v=Deno.env.get('SUPABASE_URL')||'';if(!v)throw Object.assign(new Error('SUPABASE_URL unavailable.'),{status:500});return v.replace(/\/+$/,'')}
function secretKey(){const v=Deno.env.get('SUPABASE_SECRET_KEY')||namedKey('SUPABASE_SECRET_KEYS')||Deno.env.get('SUPABASE_SERVICE_ROLE_KEY')||'';if(!v)throw Object.assign(new Error('Supabase privileged key is unavailable.'),{status:500});return v}
function adminClient(){return createClient(projectUrl(),secretKey(),{auth:{persistSession:false,autoRefreshToken:false}})}

Deno.serve(async req=>{const pre=preflight(req);if(pre)return pre;try{const admin=adminClient(),body=await req.json().catch(()=>({})),ownerEmail=String(body.ownerEmail||'').trim().toLowerCase(),pairingCode=String(body.pairingCode||'').trim().toUpperCase(),installationId=String(body.installationId||'').trim();if(!ownerEmail||!pairingCode||!installationId)return json({success:false,code:'PAIRING_FIELDS_REQUIRED',error:'Owner email, pairing code, and station installation ID are required.'},400);if(!/^[A-Fa-f0-9-]{36}$/.test(installationId))return json({success:false,code:'INSTALLATION_ID_INVALID',error:'Station installation identity is invalid.'},400);const hash=await sha256(pairingCode),now=new Date().toISOString();const{data:pairing,error:pairError}=await admin.from('station_pairing_codes').select('*').eq('code_hash',hash).is('used_at',null).gt('expires_at',now).maybeSingle();if(pairError)throw pairError;if(!pairing)return json({success:false,code:'PAIRING_EXPIRED',error:'Station pairing code is invalid or expired.'},409);if(String(pairing.owner_email||'').toLowerCase()!==ownerEmail)return json({success:false,code:'OWNER_EMAIL_MISMATCH',error:'This pairing code does not belong to that business owner email.'},403);const{data:org,error:orgError}=await admin.from('organizations').select('name,lifecycle_status').eq('id',pairing.organization_id).maybeSingle();if(orgError)throw orgError;if(!org||!['active','grace_period'].includes(String(org.lifecycle_status||'active')))return json({success:false,code:org?.lifecycle_status==='terminated'?'BUSINESS_TERMINATED':'BUSINESS_SUSPENDED',error:org?.lifecycle_status==='terminated'?'This business has been terminated.':'Cloud access for this business is suspended.'},403);const{data:branch,error:branchError}=await admin.from('branches').select('name').eq('id',pairing.branch_id).maybeSingle();if(branchError)throw branchError;const{data:station,error:stationError}=await admin.from('branch_stations').select('local_id,label,pc_number,station_device_id').eq('branch_id',pairing.branch_id).eq('local_id',pairing.local_station_id).maybeSingle();if(stationError)throw stationError;if(!station)return json({success:false,code:'STATION_NOT_FOUND',error:'The PC assigned to this pairing code no longer exists.'},404);if(station.station_device_id)return json({success:false,code:'STATION_ALREADY_PAIRED',error:'This PC is already paired to another Customer Station installation.'},409);const rawToken=token(32),tokenHash=await sha256(rawToken),topicKey=token(24),stationName=String(station.label||station.pc_number||'Customer Station').slice(0,120);let device:any=null;const existing=await admin.from('station_devices').select('id,revoked_at').eq('installation_id',installationId).maybeSingle();if(existing.error)throw existing.error;if(existing.data){if(!existing.data.revoked_at)return json({success:false,code:'INSTALLATION_ALREADY_PAIRED',error:'This Customer Station installation is already paired. Reset its Cloud pairing before pairing again.'},409);const updated=await admin.from('station_devices').update({organization_id:pairing.organization_id,branch_id:pairing.branch_id,local_station_id:pairing.local_station_id,station_name:stationName,device_token_hash:tokenHash,realtime_topic_key:topicKey,status:'paired',cloud_last_seen_at:now,revoked_at:null,updated_at:now}).eq('id',existing.data.id).select('id').single();if(updated.error)throw updated.error;device=updated.data}else{const inserted=await admin.from('station_devices').insert({organization_id:pairing.organization_id,branch_id:pairing.branch_id,local_station_id:pairing.local_station_id,installation_id:installationId,station_name:stationName,device_token_hash:tokenHash,realtime_topic_key:topicKey,status:'paired',cloud_last_seen_at:now}).select('id').single();if(inserted.error)throw inserted.error;device=inserted.data}const claimed=await admin.from('station_pairing_codes').update({used_at:now,used_by_station_device_id:device.id}).eq('id',pairing.id).is('used_at',null).select('id').maybeSingle();if(claimed.error)throw claimed.error;if(!claimed.data){await admin.from('station_devices').update({status:'revoked',revoked_at:now}).eq('id',device.id);return json({success:false,code:'PAIRING_USED',error:'Station pairing code was already used.'},409)}const stationPatch=await admin.from('branch_stations').update({station_device_id:device.id,cloud_connection_status:'online',cloud_last_seen_at:now,updated_at:now}).eq('branch_id',pairing.branch_id).eq('local_id',pairing.local_station_id);if(stationPatch.error)throw stationPatch.error;return json({success:true,stationId:device.id,stationToken:rawToken,realtimeTopicKey:topicKey,organizationId:pairing.organization_id,organizationName:org.name,branchId:pairing.branch_id,branchName:branch?.name||'Main Branch',localStationId:pairing.local_station_id,stationName,pairedAt:now},201)}catch(e){return fail(e,'Unable to pair Customer Station.')}})
